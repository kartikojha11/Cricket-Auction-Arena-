import {roleCounts} from "./validation.js";
export function scoreTeam(teamSize,squad,playersById){const ps=(squad||[]).map(id=>playersById[id]).filter(Boolean),n=ps.length||1,avg=k=>ps.reduce((s,p)=>s+(p[k]||0),0)/n;const batting=avg("battingRating"),bowling=avg("bowlingRating"),ars=ps.filter(p=>p.role==="All-rounder"),allRound=ars.length?ars.reduce((s,p)=>s+p.overallRating,0)/ars.length:0,balance=100-Math.min(100,Math.abs(batting-bowling));const req=teamSize===5?{Wicketkeeper:1,Batter:1,Bowler:1,"All-rounder":0}:{Wicketkeeper:1,Batter:4,Bowler:3,"All-rounder":1},counts=roleCounts(squad,playersById),required=Object.values(req).reduce((a,b)=>a+b,0),covered=Object.entries(req).reduce((s,[r,m])=>s+Math.min(counts[r],m),0),roleCoverage=required?covered/required*100:0,finalScore=batting*.3+bowling*.3+allRound*.2+balance*.1+roleCoverage*.1;return{batting:+batting.toFixed(2),bowling:+bowling.toFixed(2),allRound:+allRound.toFixed(2),balance:+balance.toFixed(2),roleCoverage:+roleCoverage.toFixed(2),finalScore:+finalScore.toFixed(2)}}
