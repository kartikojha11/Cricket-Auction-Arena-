const e=s=>String(s).replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[m]));export function lobbyView({room,isHost}){const ps=Object.values(room.players||{});return `<section class="card hero-card"><div class="eyebrow">PRIVATE ROOM</div><h1>Room <span class="accent">${window.__roomCode}</span></h1><button class="copy-btn">Copy code</button><p class="muted">Playing ${room.teamSize} · ${ps.length}/5 players</p></section><section class="card"><h2>Players</h2><div class="player-list">${ps.map(p=>`<div class="list-row"><span><b>${e(p.name)}</b>${p.id===room.hostId?'<small> HOST</small>':''}</span><span class="dot ${p.connected?'online':''}"></span></div>`).join("")}</div>${isHost?`<button id="startBtn" class="btn primary" ${ps.length<3?'disabled':''}>START AUCTION</button>`:'<p class="muted">Waiting for the host to start…</p>'}<button id="leaveBtn" class="btn ghost">LEAVE ROOM</button></section>`}
