import assert from "node:assert/strict";import {canBid,canBuyPlayer} from "../game/validation.js";assert.equal(canBid({amount:70,currentBid:60,basePrice:20,budget:100}),true);assert.equal(canBid({amount:60,currentBid:60,basePrice:20,budget:100}),false);assert.equal(canBid({amount:120,currentBid:60,basePrice:20,budget:100}),false);assert.equal(canBuyPlayer(["p01"],"p01"),false);assert.equal(canBuyPlayer(["p01"],"p02"),true);console.log("auction.test.js PASS");
