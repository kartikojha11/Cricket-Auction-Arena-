export function resultsView({room,scores}){const rows=Object.values(room.players||{}).map(p=>({p,s:scores[p.id]})).sort((a,b)=>b.s.finalScore-a.s.finalScore);return `<section class="card result-hero"><div class="eyebrow">FINAL RESULTS</div><h1>Auction Complete</h1><p class="muted">Deterministic team scoring · no AI involved</p></section><section class="card"><h2>Scores</h2>${rows.map((x,i)=>`<div class="result-row"><div><span class="rank">#${i+1}</span><b>${x.p.name}</b><small>${x.p.squad.length} players · ₹${x.p.spent} spent</small></div><strong>${x.s.finalScore}</strong></div>`).join("")}</section><section class="card"><h2>Score Breakdown</h2>${rows.map(x=>`<details><summary>${x.p.name} — ${x.s.finalScore}</summary><div class="breakdown"><span>Batting 30% <b>${x.s.batting}</b></span><span>Bowling 30% <b>${x.s.bowling}</b></span><span>All-round 20% <b>${x.s.allRound}</b></span><span>Balance 10% <b>${x.s.balance}</b></span><span>Role Coverage 10% <b>${x.s.roleCoverage}</b></span></div></details>`).join("")}</section><button id="playAgainBtn" class="btn primary">CREATE NEW ROOM</button>`}
