export function auctionView({room,me,player,remaining}){const min=Math.max(player?.basePrice||0,Number(room.auction.currentBid||0)+5);return `<section class="auction-card"><div class="eyebrow">LIVE AUCTION · LOT ${room.auction.playerIndex+1}</div><h1>${player?.name||"Loading…"}</h1><span class="role-badge">${player?.role||""}</span><div class="price-grid"><div><span>Base</span><strong>₹${player?.basePrice||0}</strong></div><div><span>Current</span><strong>₹${room.auction.currentBid||0}</strong></div></div><div class="timer">${remaining}s</div><p class="muted">Highest bidder: <b>${room.auction.highestBidderId?(room.players?.[room.auction.highestBidderId]?.name||"Player"):"No bids yet"}</b></p><div class="bid-row"><input id="bidInput" type="number" min="${min}" step="5" value="${min}" inputmode="numeric"><button id="bidBtn" class="btn primary">BID</button></div><button id="passBtn" class="btn ghost">PASS</button></section><section class="stats-grid"><div class="stat"><span>My Budget</span><b>₹${me.budget}</b></div><div class="stat"><span>Spent</span><b>₹${me.spent}</b></div><div class="stat"><span>Squad</span><b>${me.squad.length}</b></div></section><section class="card"><h2>My Squad</h2><div class="chips">${me.squad.map(id=>`<span>${room.__players?.[id]?.name||id}</span>`).join("")||'<span class="muted">No players yet</span>'}</div></section>`}
